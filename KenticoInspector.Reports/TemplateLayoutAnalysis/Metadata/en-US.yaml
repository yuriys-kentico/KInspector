details:
  name: Templates with identical layouts
  shortDescription: Returns a list of identical page layouts.
  longDescription: |
    Returns the list of templates with identical custom layouts (whitespace sensitive).
terms:
  goodSummary: No identical page layouts found.
  informationSummary: <count> identical page <count:layout|layouts> found.
  tableNames:
    identicalPageLayouts: Identical page layouts
