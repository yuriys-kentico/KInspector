details:
  name: Reset User
  shortDescription: Reset a user so that you can log in using that user with an empty password.
  longDescription: |
    This ensures that a user is enabled, their password is blank, and their last changed date is reset to today.
terms:
  invalidOptions: The options provided were invalid.
